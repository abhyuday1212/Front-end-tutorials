function avgOfNum (x,y){
return Math.round((x+y)/2)
}

let a = 1;
let b = 3.5;
let c = 4;

console.log(" The Average will be equal to ", avgOfNum(a,b))
console.log(" The Average will be equal to ", avgOfNum(b,c))
console.log(" The Average will be equal to ", avgOfNum(a,c))




const sum = (p,q) => {
    return a + b + c;
}

console.log("Sum Of 3 no. is",sum)

